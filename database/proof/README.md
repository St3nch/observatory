# DB-4 Proof Records

Runtime PostgreSQL evidence is written only beneath the ignored owner-local `.database-proof/` root.

This directory stores schemas and reviewed redacted records only. A runtime record may be promoted into `accepted/` only after validation and human review. Existing execution records are immutable; corrections use a separate supersession record.
