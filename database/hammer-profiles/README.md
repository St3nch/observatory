# Hammer profiles

The eight active closed JSON manifests define DB-4 preconditions, real-spine behavior, hostile candidates, role/RLS behavior, concurrency, migration history, cleanup, and semantic restore verification.

The retired `_v1` profile family is not authoritative and must not reappear. Static profile tests prove manifest structure only; achieved `real_postgres_disposable_pass` still requires the separately owner-authorized live campaign.
